
install:
    - wget https://download.pytorch.org/libtorch/nightly/cpu/libtorch-shared-with-deps-latest.zip
    - unzip -f libtorch-shared-with-deps-latest.zip

cmake_prefix:
    - libtorch/share/cmake/

