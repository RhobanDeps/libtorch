
install:
    - wget https://download.pytorch.org/libtorch/cpu/libtorch-cxx11-abi-shared-with-deps-1.11.0%2Bcpu.zip -O libtorch.zip
    - unzip -u libtorch.zip
    - rm libtorch.zip

cmake_prefixes:
    - libtorch/share/cmake/

